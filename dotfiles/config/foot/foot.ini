# foot terminal config
# Unset options fall back to foot's built-in defaults.

[colors]
# 1.0 = opaque, 0.0 = fully transparent. Applies to the background only.
alpha=0.92
background=000000
foreground=ffffff
