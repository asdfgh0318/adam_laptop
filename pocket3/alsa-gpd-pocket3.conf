# /etc/modprobe.d/alsa-gpd-pocket3.conf
# The 1195G7 Pocket 3 has no working SOF topology; force the legacy HDA path.
# The N6000 (Jasper Lake) works with SOF on kernel >= 6.1 and must NOT get this.
options snd-intel-dspcfg dsp_driver=1
